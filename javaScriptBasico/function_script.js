function sayHello(name, lastName) {
  console.log('Hello ' + name + ' ' + lastName);
}

sayHello("Karina", "Vtangelo");
// as funções sempre precisam ser executadas / chamadas.
// No exemplo acima, passamos as variáveis name e lastName como parâmetros da função e ao solicitar a execução dessa função atribuimos os valores a essas variáveis.


function sum(a,b) {
  return a + b
}
console.log(sum(40 , 10));

